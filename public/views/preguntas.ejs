<!DOCTYPE html>
<html lang="es">

<head>
    <title> Preguntas - FaceBluff </title>
    <link rel="stylesheet" type="text/css" href="css/preguntas.css" media="screen">
    <link rel="shortcut icon" href="../img/icon.png">


</head>

<body>
    <%- include('./header'); %>
    <h1 class="tituloPreguntas">Preguntas</h1>
    <div class="panelPreguntas">
        <%if(lista_preguntas[0]){%>
        <p>Preguntas seleccionadas aleatoriamente:</p>
        <%}else{%>
        <p>Parece que nadie ha creado preguntas.</p>
        <%}%>
        <%lista_preguntas.forEach(pregunta=>{%>
        <div>
            <ul>

                <li>
                    <form method="GET" action="/desarrollo_pregunta">
                        <input type="hidden" name="id_pregunta" value="<%=pregunta.id%>">
                        <input class="input_p" type="submit" value="<%=pregunta.texto_pregunta%>">
                    </form>
                </li>
            </ul>
        </div>
        <% }); %>



        <div id="nuevaPregunta">
            <p>¡Añade tus preguntas!</p>
            <div>
                <form method="GET" action="/nuevaPregunta">
                    <input class="input_p" type="submit" value="Crear nueva pregunta">
                </form>
            </div>
        </div>
    </div>
</body>

</html>